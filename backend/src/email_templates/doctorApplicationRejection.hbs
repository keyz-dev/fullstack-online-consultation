<html>
  <head>
    <meta charset="utf-8" />
    <title>Doctor Application Update</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        background-color: #f5f5f5;
      }
      .container {
        background: #ffffff;
        border-radius: 8px;
        padding: 30px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }
      .header {
        text-align: center;
        margin-bottom: 30px;
      }
      .logo {
        height: 60px;
        margin-bottom: 20px;
      }
      .status {
        background: #dc3545;
        color: white;
        padding: 10px 20px;
        border-radius: 20px;
        font-weight: bold;
        display: inline-block;
        margin-bottom: 20px;
      }
      .content {
        margin-bottom: 25px;
      }
      .details {
        background: #f8f9fa;
        padding: 20px;
        border-radius: 6px;
        margin: 20px 0;
      }
      .detail-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 8px;
        padding-bottom: 8px;
        border-bottom: 1px solid #e9ecef;
      }
      .detail-row:last-child {
        border-bottom: none;
        margin-bottom: 0;
      }
      .label {
        font-weight: bold;
        color: #495057;
      }
      .value {
        color: #212529;
      }
      .reason {
        background: #f8d7da;
        border: 1px solid #f5c6cb;
        padding: 15px;
        border-radius: 6px;
        margin: 20px 0;
      }
      .cta {
        text-align: center;
        margin: 25px 0;
      }
      .button {
        background: #007bff;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 6px;
        font-weight: bold;
        display: inline-block;
      }
      .footer {
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px solid #e9ecef;
        font-size: 12px;
        color: #6c757d;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <img src="cid:logo" alt="ConsultlyPlus Logo" class="logo" />
        <div class="status">Application Status Update</div>
      </div>

      <div class="content">
        <h2>Hello {{userName}},</h2>
        <p>Thank you for your interest in becoming a doctor with ConsultlyPlus. We
          have completed our review of your application.</p>

        <div class="details">
          <div class="detail-row">
            <span class="label">Application ID:</span>
            <span class="value">{{applicationId}}</span>
          </div>
          <div class="detail-row">
            <span class="label">Business Name:</span>
            <span class="value">{{businessName}}</span>
          </div>
          <div class="detail-row">
            <span class="label">Review Date:</span>
            <span class="value">{{reviewDate}}</span>
          </div>
          <div class="detail-row">
            <span class="label">Status:</span>
            <span class="value">{{status}}</span>
          </div>
        </div>

        <div class="reason">
          <strong>Reason for Decision:</strong><br />
          {{rejectionReason}}
        </div>

        {{#if adminRemarks}}
          <p><strong>Reviewer's Feedback:</strong> {{adminRemarks}}</p>
        {{/if}}

        <p>You can submit a new application after addressing the feedback
          provided.</p>

        <div class="cta">
          <a href="{{homeUrl}}" class="button">Submit New Application</a>
        </div>

        <p><strong>Need help?</strong> Contact us at support@consultlyPlus.com</p>
      </div>

      <div class="footer">
        <p>This email was sent to
          {{userEmail}}
          regarding your doctor application.</p>
        <p>© 2025 ConsultlyPlus. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>